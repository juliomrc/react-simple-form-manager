import { makeStyles } from "@material-ui/core";

export const useInputStyles = makeStyles(() => {
    return {
        input: {
            width: "100%",
            margin: "10px 0",
        },
    };
});
